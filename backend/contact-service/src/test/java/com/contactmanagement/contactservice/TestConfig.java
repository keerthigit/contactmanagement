package com.contactmanagement.contactservice;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Optional test slice hook. Not a {@code @SpringBootApplication} to avoid
 * duplicate {@code @SpringBootConfiguration} with {@link ContactServiceApplication}.
 */
@TestConfiguration
public class TestConfig {
}
